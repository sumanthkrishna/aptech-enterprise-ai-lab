# Scripts

Planned pilot helpers:

- `verify-environment` — verify Python/package/auth prerequisites.
- `reset-lab` — restore learner exercises to the clean baseline.
- defect injection helpers — instructor-only; keep outside student access before production delivery.
